# crosshair-cursor runtime has zero third-party dependencies.
# It runs on Python 3.9+ standard library only so it is safe to use
# as a Cursor hook without polluting your global Python environment.
#
# Development tooling only:
pytest>=7.0
pytest-cov>=4.0
